---
import BaseHead from '../../components/BaseHead.astro';
import Nav from '../../components/Nav.astro';
import Footer from '../../components/Footer.astro';
import AlbumCard from '../../components/AlbumCard.astro';
import { getCollection } from 'astro:content';

const allAlbums = await getCollection('albums');
const albums = allAlbums.filter(a => !a.data.listenSessionDate || a.data.listenSessionDate.trim() === '');
---

<!doctype html>
<html lang="en">
<head>
  <BaseHead title="Candidates — Echo Realms" />
  <style>
    .hero-glow {
      position: fixed;
      top: 0; left: 50%;
      transform: translateX(-50%);
      width: 600px; height: 400px;
      border-radius: 50%;
      background: radial-gradient(ellipse, oklch(0.62 0.12 28 / 0.05) 0%, transparent 65%);
      pointer-events: none;
    }
  </style>
</head>
<body>
  <Nav activePage="candidates" />
  <div class="hero-glow"></div>

  <main class="page">
    <header class="page-header">
      <h1>Candidates</h1>
      <div class="divider" style="margin-bottom:18px;"></div>
      <p>Doors not yet opened — albums awaiting their evening.</p>
      <span class="count">{albums.length} album{albums.length !== 1 ? 's' : ''}</span>
    </header>
    <div class="grid-container">
      <div class="album-grid">
        {albums.length === 0
          ? <p class="empty-state">The list is clear for now.</p>
          : albums.map(album => (
              <AlbumCard
                slug={album.id}
                artist={album.data.artist}
                title={album.data.title}
                coverURL={album.data.coverURL}
              />
            ))
        }
      </div>
    </div>
  </main>

  <Footer />
</body>
</html>
